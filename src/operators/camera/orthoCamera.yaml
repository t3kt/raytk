!rop
meta: !meta
  opType: raytk.operators.camera.orthoCamera
  opVersion: '8'
  opStatus: beta
opDef: !def
  enable: true
  typeSpec: !ropTypes
    coordType: !coordT
      Coordtypevec2: true
    contextType: !contextT
      Contexttypecameracontext: true
    returnType: !returnT
      Returntyperay: true
  function: !text
    file: src/operators/camera/orthoCamera.glsl
    name: functionTemplate
  useParams:
  - Campos[xyz]
  - Rotate
  angleParams: Rotate
  macroTable: !table
    file: src/operators/camera/orthoCamera_macros.txt
    name: macro_exprs
    evaluate: true
    evalOpts: !evalOpts {}
  generatedMacroTables: axis_macros
  help: !text
    file: src/operators/camera/orthoCamera.md
    name: help
