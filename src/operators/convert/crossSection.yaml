!rop
meta: !meta
  opType: raytk.operators.convert.crossSection
  opVersion: '2'
  opStatus: beta
opDef: !def
  enable: !expr
    expr: op('..').par.Enable
  typeSpec: !ropTypes
    coordType: !coordT
      Coordtypefloat: !expr
        expr: op('switcher_info')[1, 'coordType'] == 'float'
      Coordtypevec2: !expr
        expr: op('switcher_info')[1, 'coordType'] == 'vec2'
    contextType: !contextT
      Allcontexttype: true
    returnType: !returnT
      Allreturntype: true
  function: !text
    file: src/operators/convert/crossSection.glsl
    name: function
  useParams: Offset[xyz]
  help: !text
    file: src/operators/convert/crossSection.md
    name: help
