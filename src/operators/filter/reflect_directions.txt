name	label	dir	code
custom	Custom	1	axis = -1; dir = 1; planeNormal = THIS_Planenormal;
xpos	X+	1	axis = 0; dir = 1; planeNormal = vec3(1., 0., 0.);
xneg	X-	-1	axis = 0; dir = -1; planeNormal = vec3(-1., 0., 0.);
ypos	Y+	1	axis = 1; dir = 1; planeNormal = vec3(0., 1., 0.);
yneg	Y-	-1	axis = 1; dir = -1; planeNormal = vec3(0., -1., 0.);
zpos	Z+	1	axis = 2; dir = 1; planeNormal = vec3(0., 0., 1.);
zneg	Z-	-1	axis = 2; dir = -1; planeNormal = vec3(0., 0., -1.);
