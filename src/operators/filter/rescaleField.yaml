!rop
meta: !meta
  opType: raytk.operators.filter.rescaleField
  opVersion: '22'
  opStatus: default
opDef: !def
  enable: !expr
    expr: op('..').par.Enable
  useRuntimeBypass: true
  typeSpec: !ropTypes
    Useinputreturntype: !expr
      expr: parent().par.Returntype == 'auto'
    coordType: !coordT
      Allcoordtype: true
    contextType: !contextT
      Allcontexttype: true
    returnType: !returnT
      Returntypefloat: !expr
        expr: parent().par.Returntype in ('auto', 'float')
      Returntypevec4: !expr
        expr: parent().par.Returntype in ('auto', 'vec4')
  function: !text
    file: src/operators/filter/rescaleField.glsl
    name: functionTemplate
  paramGroupTable: eval_params
  help: !text
    file: src/operators/filter/rescaleField.md
    name: help
  keywords:
  - rescale
  - remap
  - range
  shortcuts: rf
  displayCategory: Field Modifier
inputs:
- !input
  Required: true
  coordType: !coordT
    Allcoordtype: true
  contextType: !contextT
    Allcontexttype: true
  returnType: !returnT
    Returntypefloat: true
    Returntypevec4: !expr
      expr: parent().par.Returntype in ('auto', 'vec4')
