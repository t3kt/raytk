	'THIS_SWIZZLE'	parent().par.Axisx.eval().replace('zero', 'x') + parent().par.Axisy.eval().replace('zero', 'x') + parent().par.Axisz.eval().replace('zero', 'x')
	'THIS_MASK'	f"vec3({0 if parent().par.Axisx == 'zero' else 1}, {0 if parent().par.Axisy == 'zero' else 1}, {0 if parent().par.Axisz == 'zero' else 1})"
