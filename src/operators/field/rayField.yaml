!rop
meta: !meta
  opType: raytk.operators.field.rayField
  opVersion: '4'
  opStatus: default
opDef: !def
  enable: true
  typeSpec: !ropTypes
    coordType: !coordT
      Coordtypevec3: true
    contextType: !contextT
      Contexttypematerialcontext: !expr
        expr: parent().par.Contexttype in ('auto', 'MaterialContext')
      Contexttyperaycontext: !expr
        expr: parent().par.Contexttype in ('auto', 'RayContext')
    returnType: !returnT
      Returntypevec4: true
  function: !text
    file: src/operators/field/rayField.glsl
    name: function
  paramGroupTable: eval_params
  help: !text
    file: src/operators/field/rayField.md
    name: help
