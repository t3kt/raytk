!rop
meta: !meta
  opType: raytk.operators.field.chopField
  opVersion: '14'
  opStatus: default
opDef: !def
  enable: true
  typeSpec: !ropTypes
    coordType: !coordT
      Coordtypefloat: !expr
        expr: parent().par.Coordtype == 'float'
      Coordtypevec2: !expr
        expr: parent().par.Coordtype == 'vec2'
      Coordtypevec3: !expr
        expr: parent().par.Coordtype == 'vec3'
    contextType: !contextT
      Allcontexttype: true
    returnType: !returnT
      Returntypefloat: !expr
        expr: parent().par.Returntype == 'float'
      Returntypevec4: !expr
        expr: parent().par.Returntype == 'vec4'
  function: !text
    file: src/operators/field/chopField.glsl
    name: functionTemplate
  specialParams: Length
  macroParams:
  - Extendmode
  - Axis
  libraryNames: !expr
    expr: op('modZigZag')
  textureTable: !table
    file: src/operators/field/chopField_textures.txt
    name: texture_exprs
    evaluate: true
    evalOpts: !evalOpts {}
  help: !text
    file: src/operators/field/chopField.md
    name: help
inputs:
- !input
  Localalias: coordField
  coordType: !coordT
    Allcoordtype: true
  contextType: !contextT
    Allcontexttype: true
  returnType: !returnT
    Returntypefloat: true
elements:
- !opElement
  name: codeSwitcher_Coordmode
  elementType: codeSwitcher
  params:
    Table: !table
      file: src/operators/field/chopField_coordMode.txt
      name: coordModes
    Param: Coordmode
    Autoupdateparams: true
    Manageparamstates: true
    Alwaysincludeallparams: false
    Switchmode: inline
    Placeholder: ''
