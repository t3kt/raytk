!rop
meta: !meta
  opType: raytk.operators.field.multiPointDistanceField
  opVersion: '2'
  opStatus: default
opDef: !def
  enable: true
  typeSpec: !ropTypes
    coordType: !coordT
      Coordtypefloat: !expr
        expr: parent().par.Coordtype == 'float'
      Coordtypevec2: !expr
        expr: parent().par.Coordtype == 'vec2'
      Coordtypevec3: !expr
        expr: parent().par.Coordtype == 'vec3'
    contextType: !contextT
      Allcontexttype: !expr
        expr: parent().par.Contexttype == 'auto'
      Contexttypecontext: !expr
        expr: parent().par.Contexttype == 'Context'
      Contexttypematerialcontext: !expr
        expr: parent().par.Contexttype == 'MaterialContext'
      Contexttypecameracontext: !expr
        expr: parent().par.Contexttype == 'CameraContext'
      Contexttypelightcontext: !expr
        expr: parent().par.Contexttype == 'LightContext'
      Contexttyperaycontext: !expr
        expr: parent().par.Contexttype == 'RayContext'
    returnType: !returnT
      Returntypevec4: true
  function: !text
    file: src/operators/field/multiPointDistanceField.glsl
    name: functionTemplate
  useParams: !expr
    expr: '''Point1[X] Point2[X] Point3[X] Point4[X]''.replace(''X'', ''xyz''[:parent().par.Coordtype.menuIndex+1])'
  help: !text
    file: src/operators/field/multiPointDistanceField.md
    name: help
