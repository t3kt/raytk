name	enable	type	chop	uniformType	length	expr1	expr2	expr3	expr4
pointAPositions	parent().par.Source == 'chops'	vec3	op('pointA_positions')	uniformarray	parent().par.Count
pointBPositions	parent().par.Source == 'chops'	vec3	op('pointB_positions')	uniformarray	parent().par.Count
