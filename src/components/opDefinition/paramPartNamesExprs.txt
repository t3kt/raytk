[c for c in op('param_tuplets').col('part1')[1:] if c and c.owner[c.row, 'handling'] == 'runtime'] or '_'
[c for c in op('param_tuplets').col('part2')[1:] if c and c.owner[c.row, 'handling'] == 'runtime'] or '_'
[c for c in op('param_tuplets').col('part3')[1:] if c and c.owner[c.row, 'handling'] == 'runtime'] or '_'
[c for c in op('param_tuplets').col('part4')[1:] if c and c.owner[c.row, 'handling'] == 'runtime'] or '_'
